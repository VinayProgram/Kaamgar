server {
    listen 80;
    server_name localhost; # Or your domain name

    root /usr/share/nginx/html; # Points to where your static files are copied

    index index.html index.htm; # Default files to serve

    location / {
        try_files $uri $uri/ =404; # Try to serve file, then directory, otherwise 404
    }

    # Optional: Example for a simple API proxy (uncomment and modify if needed)
    # location /api/ {
    #     proxy_pass http://your_backend_service:3000/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }
}
